# Leave unset for local dev — Vite proxies /api to localhost:3000
# VITE_API_BASE_URL=https://your-api.onrender.com

# Widgets with phase <= this value are enabled
# VITE_APP_PHASE=7

# Hours before cached fixture/standings data is considered stale
# VITE_CACHE_TTL_HOURS=24
